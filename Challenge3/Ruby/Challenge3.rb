def modulus_checker
totalNumber = 600851475143;

(1..totalNumber) def |x| totalNumber = totalNumber / x if (totalNumber == 600851475143) end }

	#(1..100).each{ |x| totalNumber = (totalNumber / x) if (totalNumber % x == 0)}
puts totalNumber
end

modulus_checker()
#     for (var i = 2; i < totalNumber; i++) {
#         if (totalNumber % i == 0) {
#             totalNumber = (totalNumber / i);
#         }
#     }
#     return totalNumber;
# }

# console.log(PrimeFactors());